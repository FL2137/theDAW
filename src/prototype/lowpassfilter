import numpy as np

# int n;
#   y[0] = x[0] + xm1;
#   for (n=1; n < M ; n++) {
#     y[n] =  x[n]  + x[n-1];
#   }
#   return x[M-1];

def low_pass_filter(data: list[np.int32]):
    out = []
    for i in range(1, len(data)):
        out.append(data[i] + data[i-1])
    return out